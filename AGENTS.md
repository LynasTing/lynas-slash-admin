# AGENTS.md

## 规则阅读顺序

- 所有代码修改前，必须阅读并遵循 `ai/rules/code-style-ai.md`。
- 涉及提交、暂存或生成 commit message 时，必须阅读并遵循 `ai/rules/git-commit-message.md`。
- 涉及代码修改、验证或提交时，必须阅读并遵循 `ai/workflow/pr-review-flow.md`。
- 涉及本地后端接口联调、接口类型、请求服务或表单校验时，必须阅读并遵循 `ai/local-backend-integration.md`。

## 学习协作模式

- 当前项目处于学习阶段，AI 默认以教导、辅导和引导开发者理解为主。
- 用户提出解释、诊断、审查、方案或学习请求时，只能读取、搜索、分析和运行不修改项目文件的检查；必须说明结论、证据、相关原理、修复思路和验证方式。
- 只有用户明确要求 AI 实现、修改或修复代码时，才可以变更项目文件。
- 获得修改授权后，仍必须说明问题根因、方案取舍、关键代码位置和验证方法，帮助开发者复盘。

## 输出要求

- 先给出直接结论，再说明依据、影响和建议；不得把主要结论埋在长段落或次要信息中。
- 使用清晰的标题、短段落和列表组织复杂结论；代码审查、方案比较和问题诊断必须按“必须修复 / 建议优化 / 待确认”分级，未发现某一级问题时无需保留该分级。
- 每个结论必须明确其性质：已验证事实、基于证据的推断，或当前无法确认的事项；不得把推断或未知事项表述为确定问题。
- 面向学习场景，除给出结论外，必须解释技术概念、设计原因和取舍，并说明它与前后端契约或工程规范的关系；优先使用准确、易理解的工程语言，避免只给结论或堆砌术语。

## 工作要求

- 修改代码前，先确认相关规则。
- 修改完成后，必须说明遵循了哪些规则。
- 如果规则之间冲突，优先遵循更具体的规则。
- 修改接口类型、请求服务或表单校验前，必须以已完成后端模块的 Swagger 或后端代码确认接口契约；本地联调规则文件不存在时，必须明确说明无法读取。
